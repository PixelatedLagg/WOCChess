todo:
-add pawn promotion
-add en passante (no idea how to spell it)
-add castling short and long



BitBoards[piece] &= ~(1U << from); //reset bit at previous position
BitBoards[piece] |= 1U << to; //set bit at current position

game.WhiteRooks |= temp >> 8; //add temp rook
game.WhiteRooks ^= rook; //remove previous rook

verification includes:
-is correct turn
-piece exists
-piece isnt moving into check (either by pin or king moving into check)
